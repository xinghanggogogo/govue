import actions from './action'
import mutations from './mutations'
import * as getters from './getters'

const state = {
	itemTagModuleB: '这是moduleB初始的state'
}

export default {
	state,
	actions,
	mutations,
	getters
}
